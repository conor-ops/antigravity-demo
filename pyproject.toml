[build-system]
requires = ["uv_build>=0.10.8,<0.11"]
build-backend = "uv_build"

[project]
name = "waverunner-llamaindex"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "llama-index-workflows>=2.20.0",
    "python-dotenv>=1.2.2",
    "typer>=0.25.1",
]

[project.scripts]
wavellama = "waverunner_llamaindex.cli:app"
